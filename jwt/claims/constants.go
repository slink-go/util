package claims

const (
	UserId = "UserId"
	//UserName  = "UserName"
	AuthnType = "AuthnType" // { local | delegated }
	//AuthnProvider = "AuthProvider" // authentication provider name for multiple authenticators support
	TenantId = "TenantId"
)

const (
	AuthnTypeLocal     = "local"
	AuthnTypeDelegated = "delegated"
)
